@{
    ViewBag.Title = "HelpDoc CMS";
}

<div ng-app="post.module">
    <div class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                @Html.ActionLink("HELPDOC", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
            </div>
            <div class="navbar-collapse collapse"  ng-controller="nav.controller">
                <ul class="nav navbar-nav">
                    <li class="active"><a href="#" ng-click="loadHomePage()">Home</a></li>
                    <li><a href="#" ng-click="loadAddView()">NEW POST</a></li>
                    <li ng-controller="auth.controller" class="pull-right">
                        <a href="#" ng-click="logout()"
                           ng-show="showLogout">Logout {{username}}</a>
                    </li>
                </ul>
            </div>
        </div>
    </div>

    <div ng-view class="container">

    </div>

</div>

